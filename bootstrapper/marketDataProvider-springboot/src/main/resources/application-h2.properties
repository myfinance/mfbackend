# Spring Boot will by default scan the properties files \u201capplication.properties\u201d. 
# Spring Boot also will by default load properties files for specific active profiles. 
# By following the naming convention of \u201capplication\u201d, dash (-), profile name, dot (.) properties for the file name, 
# Spring Boot will automatically load properties files specific to the active profiles.

#Inmemory-DB nur f\u00fcr unittests zu gebrauchen da Zugriff nur innerhalb des Prozesses bzw \u00fcber h2-console m\u00f6glich


#spring.datasource.url=jdbc:h2:mem:testdb;Mode=Oracle
#Set the datasource platform to \u2018H2\u2019. This enables Spring Boot to run the SQL script schema-h2.sql  on startup.
#spring.datasource.platform=h2
#Override the default behavior of Hibernate to not create and drop the database. (ddl-auto=none). 
# sonst w\u00fcrde die Datenbank mit schema-h2.sql angelegt und anschliessend wieder gel\u00f6scht und mit aus hibernate generiertem code erzeugt
#spring.jpa.hibernate.ddl-auto=none
#spring.jpa.hibernate.ddl-auto=create-drop
#Allow Hibernate to continue on error. In the SQL script we\u2019re asking to drop objects which may or may not be there. We\u2019re setting this to true, to ignore the errors and continue.
#spring.datasource.continue-on-error=true
