apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mfbackend.fullname" . }}-test-connection"
  labels:
    {{- include "mfbackend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mfbackend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
